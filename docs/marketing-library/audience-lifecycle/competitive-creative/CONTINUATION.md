# #426 competitive creative research checkpoint

Version: 2026-09-19.1. Status: research/reference, not live activation.

## Where to continue

Repository: `CGProductionHouse/CG-Dynamics`.
Base reviewed: `73a90a1213678b79a2fd0ae8c8de3043ca7da019` (research PRs #427 and #428 merged).
Research branch: `research/426-competitive-creative-2026-09-19`.
Folder: `docs/marketing-library/audience-lifecycle/competitive-creative/`.
Parent authority: Issue #426, including founder comment `5741846208`; supervisor #381.

Refresh current main, issue comments and ownership before acting. A separate worker owns the FIRST pack's `feat/426-marketing-intelligence-library-bridge`. This new chapter does not enlarge that worker's mission. Do not modify its source manifest, generator, runtime files or branch.

## Six repository source files

`RESEARCH.md` contains the scoped native/paid-tool comparison, field-level evidence framework, research protocol, two bounded case reports, original adaptation method, reference packet, integration stages and procurement test.

`evidence.json` contains 22 records: 5 full-page text reads, 11 selected-section reads, 3 indexed excerpts, 2 interface-only probes and 1 pricing page without an accepted price. Eight knowledge candidates remain needs_review. A historical announcement is not a current endpoint test; a case article is not video inspection. These files contain no copied competitor media, private client context or credential data.

`reference.mjs` and `reference.test.mjs` provide pure editorial checks, native-identity deduplication, benchmark coverage and a capacity-value scenario. `build_hub.py` creates the offline reader using the exact same reference logic. This file is the handoff. The additional `browser-check.py` is in the downloadable local export only: its GitHub upload was blocked because the connector could not determine the request safety status. No retry or alternate upload of that script was attempted.

## Verification actually performed

- Node 22.16: 54 tests passed, zero failed/skipped.
- Standard-library Python hub build passed; generated HTML is 64,449 bytes.
- Playwright with installed Chromium: 1440x1000 and 390x844; all eleven destinations; evidence warnings; sample rejection; cost calculations; blank-input rejection without stale values; indexed-only filtering; publisher search; no horizontal body overflow; no JavaScript page errors.
- Desktop evidence lab and mobile overview screenshots visually inspected. No competitor video or authenticated Dynamics screen was tested.
- Browser loaded supplied HTML with `set_content`; this does not prove deployed hosting or provider access.
- Full repository clone unavailable because container DNS could not resolve GitHub. No full application build, app CI or production test is claimed. No application files changed.
- Committed-source byte equivalence should be checked with Git blob SHAs after writes; the PR/issue checkpoint records the completed comparison.

## Reproduce

From this folder:

```sh
node --test reference.test.mjs
python3 build_hub.py
```

Optional browser verification in the downloadable export requires Playwright and Chromium in the local development environment. The export-only `browser-check.py` uses CHROMIUM_PATH, a detected system browser, or Playwright's installed Chromium. It does not add a production dependency. This script is not claimed to be present in the repository.

```sh
python3 browser-check.py
```

Generated HTML, local test logs and screenshots are exported artifacts, not a second maintained research authority. Rebuild them from the versioned files. The HTML has no external scripts, fonts, trackers, API calls or persistence. Explicit source links open externally only when a user selects them.

## First product implementation gate

Reconcile the Control Centre and live owners. Inspect current canonical source and review models. Prepare an idempotent import preview which preserves access coverage, vendor estimates, regional API limitations and native identity. Do not auto-activate indexed or unreviewed sources.

Then prove one authorised exact-client reference packet producing two original staff-reviewed concepts through the existing Content Guideline, `monthly_deliverables` and monthly strategy #391. Integrate own outcome feedback through the existing provider and #405 boundaries. No new CRM, schedule, performance ledger or knowledge authority.

## Session limitations and continuity

CG Dynamics tools were not discoverable; no staff bootstrap or live client/Library read is claimed. Google Control Centre read again returned 403 PERMISSION_DENIED; it was not updated. No external worker was dispatched. Scope is isolated research/reference files only.

The latest supervisor already records shared ops-handover snapshot drift. Request reconciliation of this chapter's PR/path/status into the existing Control Centre and `docs/ops/CG-DYNAMICS-OPS-HANDOVER.md` in the next authorised continuity pass. Do not overwrite global ownership or production state from this reference pack.

## Protected gates unchanged

No merge/deploy, production migration/data import, Library activation, strategy/client write, provider consent/API credentials, ads/spend, trial/subscription, store/pricing, external send, OneDrive structure/share changes or competitor-media copying occurred. A research finding and a passing reference test do not authorise any such action.

Next research validation: an authenticated native-first category pilot for one exact approved client brief, with source-complete references and staff review. No claim that native tools achieve 70–80% coverage or that any vendor has independently proven accuracy is supported yet.
