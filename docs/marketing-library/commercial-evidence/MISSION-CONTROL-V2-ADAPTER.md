# Mission Control V2 — reusable adapter and daily execution contract

Checkpoint: 19 September 2026. Extends the existing commercial-research handoff in PR #431. This file contains public-safe architecture only, not management actuals, private operating state, the full research interface or a new production implementation lane.

## Delivered file-mode capability

The owner-requested V2 working files preserve the original research archive and add a separate JSON operating snapshot. The interface supports date-selected physical actions, parallel owner/people/agent lanes, evidence-gated outcomes, dependencies, carry-forward, short-sprint replanning, source annotations, multi-engine scenario calculations and explicit state/brief export.

A local updater implements preview-by-default, expected revision, idempotency, an exclusive writer lock, atomic replacement and read-back. It does not create or complete native tasks, send messages, launch campaigns, record ledger transactions or deploy anything. A read-only same-origin API adapter is prepared; no owner API or server authentication has been deployed.

Verification reported by the producing session: 73 V2 behavioural tests, 96 retained original tests and 88 desktop/mobile DOM/preservation checks passed. The browser renders supplied local HTML using Chromium set_content because URL navigation is blocked by the container policy. This is not physical-device, authenticated Dynamics, Windows-launcher or live-orchestration acceptance. All 25 original research-package files remain byte-identical to the supplied archive.

## Critical private/public boundary

The full V2 management artifact and proposed state were supplied as files to CA. They were NOT committed to this public repository or uploaded as a new tracker to the communal business Drive. The existing V1 research location is described in `docs/ops/COMMERCIAL-RESEARCH-HANDOFF-2026-09-19.md`; do not assume V2 is installed there merely because V1 exists.

An unshared folder or private repository accessed through a communal company identity is not a separate owner-only security boundary. Staff must receive deliberately allowlisted execution instructions, not the management research, financial actuals, tax, personal money or sensitive forecasts. A client-side privacy flag is not authorisation. Real role/tenant/field enforcement must be server-side, deny by default and tested with different identities.

## Bind to existing authorities, not another tracker

The next authorised owner-side coordinator must read the actual current private tracker and the existing Master Business Agent before importing anything. The provided JSON is a proposal/template, not permission to overwrite live actuals or completed history. Resolve exact storage and source identifiers. Keep Microsoft-backed work/calendar in their freshness authority, CG-native client/package/schedule/guideline data in Dynamics, financial records in their existing accounting authority and current code ownership in GitHub/control-plane records.

The producing session could not access the other environment's private document/tracker, and its automation list did not contain the Master Business Agent. It created no replacement automation and did not change a staff assistant. CG Dynamics runtime tools were not discoverable, so no staff bootstrap or native runtime write is claimed.

## Daily execution loop

Read current authorised state and source freshness. Combine the ordinary business/admin/professional brief with the highest-value physical owner moves and separately owned parallel lanes. Two primary owner moves are a presentation default, not an artificial capacity limit. Proposed allocations are not accepted native assignments. Running agents require current accepted scope and execution receipts.

After a result, update the authorised native source first and read it back. Record its receipt, evidence status and management consequence against the exact mission item. Prepared, sent, won, invoiced, earned, collected, pushed, deployed and accepted remain distinct. Preserve unfinished work and generate the next short sprint from evidence instead of auto-extending a static annual checklist.

Keep the existing Master Business Agent's timing and normal obligations. Do not add a second reminder. Protected spending, publishing, email, pricing, credentials, tax, production and permission gates remain unchanged.

## Concurrency and security acceptance

A remote connector without an atomic revision precondition does not acquire distributed concurrency safety from a read-before-write. Use the existing transactional owner backend, or a designated single writer with immutable proposed updates and reconciliation. Local file locks protect cooperating local processes only.

Before calling the system live, prove owner access, staff denial, exact-source identity, source-error handling, concurrent-write conflict, replay safety, durable audit identity, native read-back and one complete existing morning-brief -> owner response -> persisted update -> evening-closeout cycle. No screenshot or unit-test count replaces this proof.

## Ownership

No existing Library bridge, exact-client pilot, Brand Hub, email, provider, Hours or Website Operations implementation file was changed. No worker was dispatched. No merge, production deployment, migration, data mutation, campaign, spend, source activation or staff permission change was performed by this V2 artifact checkpoint. Continue the existing PR and owning supervisor; do not create an overlapping application mission.
